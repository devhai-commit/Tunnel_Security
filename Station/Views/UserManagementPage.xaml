<Page
    x:Class="Station.Views.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Station.ViewModels"
    Background="{StaticResource BackgroundSecondaryBrush}">

    <Page.DataContext>
        <vm:UserManagementViewModel />
    </Page.DataContext>

    <Page.Resources>
        <!-- Chip / badge dùng cho System / Custom -->
        <Style x:Key="ChipBadgeBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="10,3" />
            <Setter Property="MinHeight" Value="22" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#33000000" />
        </Style>

        <Style x:Key="ChipBadgeTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="PlaceholderForeground" Value="{StaticResource TextSecondaryBrush}" />
        </Style>

        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        </Style>

        <!-- Tinh chỉnh ListViewItem cho roles (nền chọn + bo góc) -->
        <SolidColorBrush x:Key="ListViewItemBackground"
                         Color="Transparent" />
        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver"
                         Color="Transparent" />
        <SolidColorBrush x:Key="ListViewItemBackgroundPressed"
                         Color="Transparent" />
        <SolidColorBrush x:Key="ListViewItemBackgroundSelected"
                         Color="#1A3B82F6" />
        <!-- nền chọn nhạt -->
        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver"
                         Color="#263B82F6" />
        <SolidColorBrush x:Key="ListViewItemBorderBrushSelected"
                         Color="#3B82F6" />
        <CornerRadius x:Key="ListViewItemCornerRadius">14</CornerRadius>
        <Thickness x:Key="ListViewItemBorderThickness">1</Thickness>
        <Thickness x:Key="ListViewItemPadding">0</Thickness>

    </Page.Resources>

    <ScrollViewer Background="{StaticResource BackgroundSecondaryBrush}">
        <StackPanel Padding="32,24" Spacing="20">

            <!-- HEADER -->
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Quản trị người dùng"
                               FontSize="26"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Quản lý tài khoản, vai trò và phân quyền chức năng"
                               Foreground="{StaticResource TextSecondaryBrush}" />
                    <TextBlock Text="{Binding LoadStatus}"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="12" />
                </StackPanel>

                <Button Grid.Column="1"
                        Click="ViewAuditLogs_Click"
                        Style="{StaticResource SubtleButtonStyle}"
                        Padding="14,8"
                        VerticalAlignment="Top"
                        ToolTipService.ToolTip="Xem nhật ký thao tác">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Library" />
                        <TextBlock Text="Nhật ký"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>

            <!-- SEGMENTED TABS -->
            <Border Margin="0,24,0,8"
                    Background="{StaticResource BackgroundPrimaryBrush}"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="1"
                    CornerRadius="24"
                    Padding="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- TAB 1: Users & Contractors -->
                    <Button x:Name="UsersTabButton"
                            Grid.Column="0"
                            Content="Quản lý người dùng"
                            FontSize="15"
                            FontWeight="SemiBold"
                            HorizontalAlignment="Stretch"
                            Padding="0,12"
                            Background="{StaticResource AccentBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            CornerRadius="20"
                            Click="UsersTabButton_Click" />

                    <!-- TAB 2: Roles & Permissions -->
                    <Button x:Name="RolesTabButton"
                            Grid.Column="1"
                            Content="Quản lý phân quyền"
                            FontSize="15"
                            FontWeight="SemiBold"
                            HorizontalAlignment="Stretch"
                            Padding="0,12"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            Foreground="{StaticResource TextSecondaryBrush}"
                            BorderThickness="0"
                            CornerRadius="20"
                            Click="RolesTabButton_Click" />
                </Grid>
            </Border>

            <!-- ============ USERS TAB ============ -->
            <StackPanel x:Name="UsersSection" Spacing="16">

                <!-- STATS CARDS -->
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Total Users -->
                    <Border Grid.Column="0"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Tất cả người dùng"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Text="{Binding TotalUsers}"
                                       FontSize="26"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>

                    <!-- Active Users -->
                    <Border Grid.Column="1"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Hoạt động"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <StackPanel Orientation="Horizontal"
                                        Spacing="8"
                                        VerticalAlignment="Center">
                                <Ellipse Width="8" Height="8"
                                         Fill="{StaticResource SeverityLowBrush}"
                                         VerticalAlignment="Center" />
                                <TextBlock Text="{Binding ActiveUsers}"
                                           FontSize="26"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Locked Users -->
                    <Border Grid.Column="2"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Khoá"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Text="{Binding LockedUsers}"
                                       FontSize="26"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- USERS TABLE -->
                <Border Background="{StaticResource BackgroundPrimaryBrush}"
                        CornerRadius="18"
                        Padding="16"
                        BorderBrush="{StaticResource BorderLightBrush}"
                        BorderThickness="1">
                    <StackPanel Spacing="12">

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SearchBox"
                                     Grid.Column="0"
                                     PlaceholderText="Tìm kiếm người dùng..."
                                     VerticalContentAlignment="Center"
                                     TextChanged="SearchBox_TextChanged"
                                     Style="{StaticResource FilterTextBoxStyle}">
                                <TextBox.Resources>
                                    <StaticResource x:Key="TextControlBackground" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="TextControlBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="TextControlBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="TextControlBorderBrush" ResourceKey="BorderLightBrush" />
                                    <StaticResource x:Key="TextControlBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                    <StaticResource x:Key="TextControlBorderBrushFocused" ResourceKey="AccentBrush" />
                                </TextBox.Resources>
                            </TextBox>

                            <ComboBox x:Name="UserRoleFilterBox"
                                      Grid.Column="1"
                                      Width="160"
                                      Style="{StaticResource FilterComboBoxStyle}"
                                      SelectionChanged="UserRoleFilter_SelectionChanged">
                                <ComboBox.Resources>
                                    <StaticResource x:Key="ComboBoxBackground" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundPressed" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrush" ResourceKey="BorderLightBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushPressed" ResourceKey="AccentBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushFocused" ResourceKey="AccentBrush" />
                                </ComboBox.Resources>
                                <ComboBoxItem Content="Vai trò: Tất cả"
                                              Tag="all"
                                              IsSelected="True" />
                            </ComboBox>

                            <ComboBox x:Name="UserStatusFilterBox"
                                      Grid.Column="2"
                                      Width="160"
                                      Style="{StaticResource FilterComboBoxStyle}"
                                      SelectionChanged="UserStatusFilter_SelectionChanged">
                                <ComboBox.Resources>
                                    <StaticResource x:Key="ComboBoxBackground" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundPressed" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrush" ResourceKey="BorderLightBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushPressed" ResourceKey="AccentBrush" />
                                    <StaticResource x:Key="ComboBoxBorderBrushFocused" ResourceKey="AccentBrush" />
                                </ComboBox.Resources>
                                <ComboBoxItem Content="Trạng thái: Tất cả"
                                              Tag="all"
                                              IsSelected="True" />
                                <ComboBoxItem Content="Đang hoạt động"
                                              Tag="active" />
                                <ComboBoxItem Content="Đã khóa"
                                              Tag="locked" />
                            </ComboBox>

                            <Button Grid.Column="3"
                                    Content="Thêm người dùng"
                                    Click="AddUser_Click"
                                    Margin="8,0,0,0"
                                    Style="{StaticResource AccentButtonStyle}" />
                        </Grid>

                        <!-- Header row -->
                        <Grid Padding="0,4"
                              Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.7*" />
                                <ColumnDefinition Width="2.7*" />
                                <ColumnDefinition Width="3.1*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="2.2*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="1.8*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="STT"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="1"
                                       Text="Tài khoản"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="2"
                                       Text="Họ tên"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="3"
                                       Text="Vai trò"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="4"
                                       Text="Quyền chức năng"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="5"
                                       Text="Trạng thái"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Grid.Column="6"
                                       Text="Thao tác"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                        </Grid>

                        <ListView x:Name="UsersListView"
                                  ItemsSource="{Binding FilteredUsers}"
                                  SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                                  Background="Transparent"
                                  BorderThickness="0">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,2" />
                                    <Setter Property="Background" Value="Transparent" />
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:UserItem">
                                    <StackPanel Spacing="0">
                                        <Grid Padding="0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="0.7*" />
                                                <ColumnDefinition Width="2.7*" />
                                                <ColumnDefinition Width="3.1*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="2.2*" />
                                                <ColumnDefinition Width="2*" />
                                                <ColumnDefinition Width="1.8*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"
                                                       Text="{x:Bind OrderNumber}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Foreground="{StaticResource TextSecondaryBrush}" />

                                            <!-- Username -->
                                            <TextBlock Grid.Column="1"
                                                       Text="{x:Bind UserName}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       TextAlignment="Center"
                                                       Foreground="{StaticResource TextPrimaryBrush}" />

                                            <!-- Full name -->
                                            <TextBlock Grid.Column="2"
                                                       Text="{x:Bind FullName}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       TextAlignment="Center"
                                                       Foreground="{StaticResource TextSecondaryBrush}" />

                                            <!-- Role -->
                                            <Border Grid.Column="3"
                                                    Background="{StaticResource BackgroundPrimaryBrush}"
                                                    CornerRadius="10"
                                                    Padding="8,2"
                                                    HorizontalAlignment="Center">
                                                <TextBlock Text="{x:Bind RoleDisplay}"
                                                           FontSize="13"
                                                           VerticalAlignment="Center"
                                                           TextAlignment="Center"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           MaxWidth="220" />
                                            </Border>

                                            <!-- Permissions -->
                                            <Border Grid.Column="4"
                                                    Background="{StaticResource BackgroundPrimaryBrush}"
                                                    CornerRadius="10"
                                                    Padding="8,2"
                                                    HorizontalAlignment="Center">
                                                <StackPanel Orientation="Horizontal"
                                                            Spacing="6"
                                                            VerticalAlignment="Center">
                                                    <TextBlock Text="{x:Bind PermissionsCount}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                                    <TextBlock Text="quyền"
                                                               FontSize="13"
                                                               Foreground="{StaticResource TextSecondaryBrush}" />
                                                </StackPanel>
                                            </Border>

                                            <!-- Status (pill gọn, chấm màu + chữ) -->
                                            <Border Grid.Column="5"
                                                    Background="{StaticResource BackgroundPrimaryBrush}"
                                                    CornerRadius="10"
                                                    Padding="6,2"
                                                    HorizontalAlignment="Center">
                                                <StackPanel Orientation="Horizontal"
                                                            Spacing="6"
                                                            VerticalAlignment="Center">
                                                    <!-- chấm màu theo StatusBrush -->
                                                    <Ellipse Width="8"
                                                             Height="8"
                                                             Fill="{x:Bind StatusBrush}"
                                                             VerticalAlignment="Center" />
                                                    <TextBlock Text="{x:Bind StatusText}"
                                                               FontSize="12"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                                </StackPanel>
                                            </Border>


                                            <!-- Actions -->
                                            <StackPanel Grid.Column="6"
                                                        Orientation="Horizontal"
                                                        Spacing="8"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center">
                                                <Button Content="Sửa"
                                                        Click="EditUser_Click"
                                                        Style="{StaticResource SubtleButtonStyle}" />
                                                <Button Content="Xóa"
                                                        Click="DeleteUser_Click"
                                                        Style="{StaticResource SubtleButtonStyle}" />
                                            </StackPanel>
                                        </Grid>
                                        <Border Height="1"
                                                Background="{StaticResource BorderLightBrush}"
                                                Opacity="0.7" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- ============ ROLES TAB ============ -->
            <StackPanel x:Name="RolesSection"
                        Spacing="16"
                        Visibility="Collapsed">

                <!-- STATS CARDS FOR ROLES TAB -->
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Total Roles -->
                    <Border Grid.Column="0"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Tất cả vai trò"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Text="{Binding TotalRoles}"
                                       FontSize="26"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Border>

                    <!-- Custom Roles -->
                    <Border Grid.Column="1"
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Vai trò hệ thống"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="8" Height="8"
                                         VerticalAlignment="Center"
                                         Fill="{StaticResource SeverityMediumBrush}" />
                                <TextBlock Text="{Binding StandardRoles}"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           Foreground="{StaticResource TextPrimaryBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </Grid>

                <!-- MAIN ROLES LAYOUT -->
                <Grid>
                    <Border
                            Background="{StaticResource BackgroundPrimaryBrush}"
                            CornerRadius="18"
                            Padding="16"
                            BorderBrush="{StaticResource BorderLightBrush}"
                            BorderThickness="1">
                        <StackPanel Spacing="12">

                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="RoleSearchBox"
                                         Grid.Column="0"
                                         PlaceholderText="Tìm kiếm vai trò..."
                                         TextChanged="RoleSearchBox_TextChanged"
                                         Style="{StaticResource FilterTextBoxStyle}">
                                    <TextBox.Resources>
                                        <StaticResource x:Key="TextControlBackground" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="TextControlBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="TextControlBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="TextControlBorderBrush" ResourceKey="BorderLightBrush" />
                                        <StaticResource x:Key="TextControlBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                        <StaticResource x:Key="TextControlBorderBrushFocused" ResourceKey="AccentBrush" />
                                    </TextBox.Resources>
                                </TextBox>

                                <ComboBox Grid.Column="1"
                                          Width="170"
                                          SelectionChanged="RoleDisplayFilter_SelectionChanged"
                                          Style="{StaticResource FilterComboBoxStyle}">
                                    <ComboBox.Resources>
                                        <StaticResource x:Key="ComboBoxBackground" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundPressed" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrush" ResourceKey="BorderLightBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushPressed" ResourceKey="AccentBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushFocused" ResourceKey="AccentBrush" />
                                    </ComboBox.Resources>
                                    <ComboBoxItem Content="Hiển thị: Tất cả"
                                                  Tag="all"
                                                  IsSelected="True" />
                                    <ComboBoxItem Content="Vai trò chuẩn"
                                                  Tag="system" />
                                    <ComboBoxItem Content="Vai trò tuỳ chỉnh"
                                                  Tag="custom" />
                                    <ComboBoxItem Content="Đã gán người dùng"
                                                  Tag="assigned" />
                                </ComboBox>

                                <ComboBox Grid.Column="2"
                                          Width="170"
                                          SelectionChanged="RoleSortBox_SelectionChanged"
                                          Style="{StaticResource FilterComboBoxStyle}">
                                    <ComboBox.Resources>
                                        <StaticResource x:Key="ComboBoxBackground" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundPointerOver" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundPressed" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBackgroundFocused" ResourceKey="BackgroundPrimaryBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrush" ResourceKey="BorderLightBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushPointerOver" ResourceKey="AccentBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushPressed" ResourceKey="AccentBrush" />
                                        <StaticResource x:Key="ComboBoxBorderBrushFocused" ResourceKey="AccentBrush" />
                                    </ComboBox.Resources>
                                    <ComboBoxItem Content="Sắp xếp: A-Z"
                                                  Tag="name_asc"
                                                  IsSelected="True" />
                                    <ComboBoxItem Content="Sắp xếp: Z-A"
                                                  Tag="name_desc" />
                                    <ComboBoxItem Content="Nhiều quyền nhất"
                                                  Tag="permissions_desc" />
                                    <ComboBoxItem Content="Nhiều người dùng"
                                                  Tag="users_desc" />
                                </ComboBox>

                                <Button Grid.Column="3"
                                        Content="Thêm vai trò"
                                        Margin="8,0,0,0"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Click="AddRole_Click"/>
                            </Grid>

                            <Grid Padding="0,4"
                                  Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.7*" />
                                    <ColumnDefinition Width="2.7*" />
                                    <ColumnDefinition Width="3.1*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="2.2*" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="1.8*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="STT"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="1"
                                           Text="Tên vai trò"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="2"
                                           Text="Mã vai trò"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="3"
                                           Text="Loại"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="4"
                                           Text="Quyền"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="5"
                                           Text="Gán user"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                <TextBlock Grid.Column="6"
                                           Text="Thao tác"
                                           HorizontalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}" />
                            </Grid>

                            <!-- ROLES LIST -->
                            <ListView x:Name="RolesListView"
                                      ItemsSource="{Binding FilteredRoles}"
                                      SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                                      SelectionMode="Single"
                                      Background="Transparent"
                                      BorderThickness="0">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,2" />
                                        <Setter Property="Background" Value="Transparent" />
                                    </Style>
                                </ListView.ItemContainerStyle>

                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:RoleItem">
                                        <StackPanel Spacing="0">
                                            <Grid Padding="0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="0.7*" />
                                                    <ColumnDefinition Width="2.7*" />
                                                    <ColumnDefinition Width="3.1*" />
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="2.2*" />
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="1.8*" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="{x:Bind OrderNumber}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />

                                                <TextBlock Grid.Column="1"
                                                           Text="{x:Bind Name}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           TextAlignment="Center"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimaryBrush}" />

                                                <TextBlock Grid.Column="2"
                                                           Text="{x:Bind Code}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           TextAlignment="Center"
                                                           Foreground="{StaticResource TextSecondaryBrush}"
                                                           TextTrimming="CharacterEllipsis" />

                                                <Border Grid.Column="3"
                                                        Style="{StaticResource ChipBadgeBorderStyle}"
                                                        Background="{x:Bind BadgeBrush}"
                                                        HorizontalAlignment="Center">
                                                    <TextBlock Style="{StaticResource ChipBadgeTextStyle}"
                                                               Text="{x:Bind KindText}" />
                                                </Border>

                                                <Border Grid.Column="4"
                                                        Background="{StaticResource BackgroundPrimaryBrush}"
                                                        CornerRadius="10"
                                                        Padding="8,2"
                                                        HorizontalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal"
                                                                Spacing="6"
                                                                VerticalAlignment="Center">
                                                        <TextBlock Text="{x:Bind PermissionsCount}"
                                                                   FontSize="13"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{StaticResource TextPrimaryBrush}" />
                                                        <TextBlock Text="quyền"
                                                                   FontSize="13"
                                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                                    </StackPanel>
                                                </Border>

                                                <Border Grid.Column="5"
                                                        Background="{StaticResource BackgroundPrimaryBrush}"
                                                        CornerRadius="10"
                                                        Padding="8,2"
                                                        HorizontalAlignment="Center">
                                                    <StackPanel Orientation="Horizontal"
                                                                Spacing="6"
                                                                VerticalAlignment="Center">
                                                        <TextBlock Text="{x:Bind UsersCount}"
                                                                   FontSize="13"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{StaticResource TextPrimaryBrush}" />
                                                        <TextBlock Text="người dùng"
                                                                   FontSize="13"
                                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                                    </StackPanel>
                                                </Border>

                                                <StackPanel Grid.Column="6"
                                                            Orientation="Horizontal"
                                                            Spacing="6"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center">
                                                    <Button Content="Sửa"
                                                            Click="EditRole_Click"
                                                            Style="{StaticResource SubtleButtonStyle}"/>
                                                    <Button Content="Xóa"
                                                            Click="DeleteRole_Click"
                                                            Style="{StaticResource SubtleButtonStyle}"/>
                                                </StackPanel>
                                            </Grid>
                                            <Border Height="1"
                                                    Background="{StaticResource BorderLightBrush}"
                                                    Opacity="0.7" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                        </StackPanel>
                    </Border>
                </Grid>

            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
