<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Station.Dialogs.AddNodeDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Thêm Thiết bị Node mới"
    PrimaryButtonText="THÊM THIẾT BỊ"
    CloseButtonText="Hủy bỏ"
    DefaultButton="Primary"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CornerRadius="16"
    Background="#0F172A"
    RequestedTheme="Dark">

    <ContentDialog.Resources>
        <LinearGradientBrush x:Key="CyberTitleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00C6FF" Offset="0.0"/>
            <GradientStop Color="#0072FF" Offset="1.0"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00F2FE" Offset="0.0"/>
            <GradientStop Color="#4FACFE" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- Tối ưu hiệu suất: Khai báo màu một lần ở đây để UI load nhanh hơn -->
        <SolidColorBrush x:Key="NeonAccent" Color="#00E5FF"/>
        <SolidColorBrush x:Key="GlassBg" Color="#0AFFFFFF"/>
        <SolidColorBrush x:Key="GlassBorder" Color="#1AFFFFFF"/>
        <SolidColorBrush x:Key="InputBg" Color="#14FFFFFF"/>
        <SolidColorBrush x:Key="InputBorder" Color="#33FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#94A3B8"/>
        <SolidColorBrush x:Key="RequiredColor" Color="#EF4444"/>

        <!-- ÉP XUNG NÚT PRIMARY: Cấm hệ thống can thiệp -->
        <Style x:Key="ForcePrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="CharacterSpacing" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{StaticResource ButtonGradient}" CornerRadius="8" MinHeight="46" Margin="0,0,8,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ÉP XUNG NÚT CLOSE -->
        <Style x:Key="ForceCloseButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#1AFFFFFF" BorderBrush="#33FFFFFF" BorderThickness="1" CornerRadius="8" MinHeight="46" Margin="8,0,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentDialog.Resources>

    <!-- Gắn Style Ép Xung vào ContentDialog -->
    <ContentDialog.PrimaryButtonStyle>
        <StaticResource ResourceKey="ForcePrimaryButtonStyle"/>
    </ContentDialog.PrimaryButtonStyle>
    <ContentDialog.CloseButtonStyle>
        <StaticResource ResourceKey="ForceCloseButtonStyle"/>
    </ContentDialog.CloseButtonStyle>

    <ContentDialog.TitleTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
                <Border Background="#1A0072FF" Width="44" Height="44" CornerRadius="12">
                    <FontIcon Glyph="&#xE710;" FontSize="20" Foreground="{StaticResource CyberTitleGradient}"/>
                </Border>
                <TextBlock Text="Thêm Node mới" FontSize="22" FontWeight="Bold" Foreground="{StaticResource CyberTitleGradient}" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>
    </ContentDialog.TitleTemplate>

    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="650" Margin="-4,0,-24,0" Padding="4,0,24,0">
        <StackPanel Spacing="24">

            <!-- ========================================== -->
            <!-- SECTION 1: THÔNG TIN NODE                  -->
            <!-- ========================================== -->
            <StackPanel Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE81E;" FontSize="18" Foreground="{StaticResource NeonAccent}"/>
                    <TextBlock Text="Thông tin cơ bản" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="12" Padding="20">
                    <!-- Tối ưu: Dùng Grid tổng thay vì nhiều StackPanel con -->
                    <Grid RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Node Name -->
                        <StackPanel Grid.Row="0" Spacing="4">
                            <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold">Tên Node <Run Foreground="{StaticResource RequiredColor}">*</Run></TextBlock>
                            <TextBox x:Name="NodeNameTextBox" PlaceholderText="Ví dụ: N1, Node-01..." Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Foreground="{StaticResource TextPrimary}" Height="40" CornerRadius="6" BorderThickness="1"/>
                        </StackPanel>

                        <!-- Line Selection -->
                        <StackPanel Grid.Row="1" Spacing="4">
                            <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold">Tuyến <Run Foreground="{StaticResource RequiredColor}">*</Run></TextBlock>
                            <ComboBox x:Name="LineComboBox" HorizontalAlignment="Stretch" PlaceholderText="Chọn tuyến" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Foreground="{StaticResource TextPrimary}" Height="40" CornerRadius="6" BorderThickness="1">
                                <ComboBoxItem Content="Tuyến A1"/>
                                <ComboBoxItem Content="Tuyến A2"/>
                                <ComboBoxItem Content="Tuyến A3"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Location -->
                        <StackPanel Grid.Row="2" Spacing="4">
                            <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold">Vị trí <Run Foreground="{StaticResource RequiredColor}">*</Run></TextBlock>
                            <TextBox x:Name="LocationTextBox" PlaceholderText="Ví dụ: R1, Khu vực 1..." Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Foreground="{StaticResource TextPrimary}" Height="40" CornerRadius="6" BorderThickness="1"/>
                        </StackPanel>

                        <!-- Description -->
                        <StackPanel Grid.Row="3" Spacing="4">
                            <TextBlock Text="Mô tả / Ghi chú" Foreground="{StaticResource TextSecondary}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBox x:Name="DescriptionTextBox" PlaceholderText="Mô tả vị trí hoặc ghi chú..." AcceptsReturn="True" TextWrapping="Wrap" MinHeight="60" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Foreground="{StaticResource TextPrimary}" CornerRadius="6" BorderThickness="1"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- ========================================== -->
            <!-- SECTION 2: CẤU HÌNH CẢM BIẾN (TỐI ƯU LOAD) -->
            <!-- ========================================== -->
            <StackPanel Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE957;" FontSize="18" Foreground="{StaticResource NeonAccent}"/>
                    <TextBlock Text="Cấu hình 7 Cảm biến" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>

                <!-- Các khối cảm biến đã được gọt bỏ bớt lớp StackPanel thừa để mở cực nhanh -->
                <StackPanel Spacing="10">

                    <!-- 1. Radar -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE701;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="1. Radar phát hiện người" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="RadarIdTextBox" Header="ID Cảm biến" PlaceholderText="RAD-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="RadarStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 2. Camera -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE714;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="2. Camera hồng ngoại" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="CameraIdTextBox" Header="ID Cảm biến" PlaceholderText="CAM-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CameraStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 3. PIR -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7C1;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="3. Cảm biến hồng ngoại (PIR)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="PirIdTextBox" Header="ID Cảm biến" PlaceholderText="PIR-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="PirStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 4. Nhiệt ẩm -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE9CA;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="4. Cảm biến nhiệt độ và độ ẩm" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="TempHumidIdTextBox" Header="ID Cảm biến" PlaceholderText="THM-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="TempHumidStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 5. Ánh sáng -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE706;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="5. Cảm biến ánh sáng" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="LightIdTextBox" Header="ID Cảm biến" PlaceholderText="LUX-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="LightStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 6. Nước -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE9F2;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="6. Cảm biến mực nước" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="WaterIdTextBox" Header="ID Cảm biến" PlaceholderText="WTR-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="WaterStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <!-- 7. Gia tốc -->
                    <Border Background="{StaticResource GlassBg}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1" CornerRadius="10" Padding="16">
                        <Grid RowSpacing="12" ColumnSpacing="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xEDA4;" FontSize="16" Foreground="{StaticResource NeonAccent}"/>
                                <TextBlock Text="7. Cảm biến gia tốc (rung động)" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                            <TextBox Grid.Row="1" Grid.Column="0" x:Name="AccelerometerIdTextBox" Header="ID Cảm biến" PlaceholderText="ACC-001" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6"/>
                            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="AccelerometerStatusComboBox" Header="Trạng thái" HorizontalAlignment="Stretch" SelectedIndex="0" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" Height="48" CornerRadius="6">
                                <ComboBoxItem Content="Hoạt động"/>
                                <ComboBoxItem Content="Tắt"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                </StackPanel>
            </StackPanel>

            <!-- INFO NOTE -->
            <Border Background="#1A00E5FF" BorderBrush="#3300C6FF" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE946;" FontSize="20" Foreground="{StaticResource NeonAccent}" VerticalAlignment="Top"/>
                    <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextPrimary}" LineHeight="20">
                        <Run Text="Mỗi node sẽ được tự động cấu hình với"/> <Run Text="7 loại cảm biến cơ bản" FontWeight="Bold" Foreground="{StaticResource NeonAccent}"/>.<LineBreak/>
                        <Run Text="Bạn có thể chỉnh sửa ID và trạng thái bật/tắt cho từng cảm biến theo thực tế."/>
                    </TextBlock>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</ContentDialog>